# FieldForge

## Branch Workflow
- `main`: production deploys (Vercel auto-builds this branch)
- `dev`: pre-production work. Push redesigns/features here first; Vercel will generate a Preview URL per push so we can test before merging into `main`.

## Setup
Follow standard Next.js instructions...
